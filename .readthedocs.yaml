version: 2

build:
  os: ubuntu-26.04
  tools:
    python: "3.13"
    rust: "latest"

python:
  install:
    - method: uv
      command: sync
      groups:
        - docs

sphinx:
  configuration: docs/conf.py
